[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
pythonpath = .
testpaths = tests
addopts = -m "not live"
markers =
    migration: applies Alembic instead of metadata.create_all
    live: hits mealty.ru; not run in default CI
